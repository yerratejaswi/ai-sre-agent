apiVersion: v1
kind: Namespace
metadata:
  name: shop
  labels:
    sre-lab: "true"
---
apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: shop
spec:
  type: NodePort
  selector:
    app: orders-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
